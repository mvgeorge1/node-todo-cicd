
version: '3'
services:
  app:
    container_name: todo-node-app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    environment:
      NODE_ENV: production
    restart: always
